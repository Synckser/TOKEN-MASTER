# Build products
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
.DS_Store

# xcodegen regenerates this; keep project.yml as source of truth.
# (Committed anyway so the repo opens in Xcode without xcodegen.)
TokenMaster.app
